function init()
    activeList = FindEntities("activebodies", true)
    
end

function update()
    -- for tag,handle in activeList do
    --     SetBodyActive(handle, true)
    -- end
end

function tick()
end